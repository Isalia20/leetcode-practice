class Solution:
    def rob(self, nums: List[int]) -> int:
        rob1, rob2 = 0, 0
        i = 0
        while i < len(nums):
            temp = max(nums[i] + rob1, rob2)
            rob1 = rob2
            rob2 = temp
            i += 1
        return rob2
        
